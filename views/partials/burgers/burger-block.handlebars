<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{isDevoured}}">
		{{#if isDevoured}}DEVOUR!{{else}}Devoured!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
