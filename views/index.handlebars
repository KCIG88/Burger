<h1>Burger-List!</h1>

<h2>Burgers not devoured!</h2>

<ul>
  {{#each burgers}}
  {{#unless isDevoured}}
  {{> burgers/burger-block isDevoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
  {{#if isDevoured}}
  {{> burgers/burger-block isDevoured=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bur">Burger Name:</label>
    <input type="text" id="bur" burger_name="name">
  </div>

  <div class="form-group">
    <label for="isDevoured">Devour status?</label><br>
    <input type="radio" name="isDevoured" value="1" checked> Undevoured<br>
    <input type="radio" name="isDevoured" value="0"> Devoured
  </div>

  <button type="submit">Add Burger</button>
</form>